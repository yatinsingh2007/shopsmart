name : Integration

on:
    push:
        branches : ["main"]
jobs:
    build:
        runs-on : ubuntu-latest
        
        steps :
            - name : Chechout
              uses : actions/checkout@v4

            - name : Install Dependencies
              run : npm install

            - name : build
              run : npm run build

    unit-test :
        runs-on : ubuntu-latest

        steps :

            - name : Checkout
              uses : actions/checkout@v4

            - name : install dependencies
              run : npm install

            - name : build
              run : npm run build